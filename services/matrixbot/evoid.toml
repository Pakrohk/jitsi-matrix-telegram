[service]
name = "matrixbot"
version = "0.1.0"

[runtime]
adapter = "maubot"
host = "0.0.0.0"
port = 8002

[engines]
schema = "native"
storage = "sqlite"
cache = "memory"
logger = "loguru"

[pipeline]
processors = ["validate", "authorize"]
