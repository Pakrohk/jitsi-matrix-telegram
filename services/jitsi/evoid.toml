[service]
name = "jitsi"
version = "0.1.0"

[runtime]
adapter = "asgi"
host = "0.0.0.0"
port = 8003

[engines]
schema = "native"
storage = "sqlite"
cache = "memory"
logger = "loguru"

[pipeline]
processors = ["validate"]
